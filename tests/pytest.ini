[pytest]
markers =
    schema: Schema and contract validation tests
    integration: Redis integration tests (requires localhost:6379)
    e2e: End-to-end tests (requires Redis + controller)
    hardware: Hardware tests (manual only)

testpaths = .
